#pragma once

// Compatibility include for callers that still use the historical file name. The directional
// Stable CSM + PCSS is the directional visibility sampling path.
#include "stable_csm_sampling.slang"
